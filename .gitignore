node_modules/
.next/
out/
dist-electron/
release/
next-env.d.ts
*.tsbuildinfo

# Founder-only signing material — must never be committed.
# The private key mints licenses; it stays in the office.
tools/keygen/private-key.pem
tools/keygen/*.pem
!tools/keygen/public-key.pem

.DS_Store
*.log

# Cached cross-build native binaries
.build-cache/

# Stray build output that lands at the repo root
/printing.js
/[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f]*.css

# Menu photos belong in the app's data folder and travel in its backups,
# not in the repository — they are the shop's content, not app assets.
/Gemini_Generated_Image_*.jpeg
